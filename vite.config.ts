import { defineConfig } from 'vite'

export default defineConfig({
  // Base public path when served in production.
  // Set to the repository name so GitHub Pages serves assets from /hello-vue/
  base: '/hello-vue/'
})
